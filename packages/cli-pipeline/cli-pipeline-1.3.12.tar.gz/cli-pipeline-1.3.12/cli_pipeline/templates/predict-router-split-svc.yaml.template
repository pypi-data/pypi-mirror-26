apiVersion: v1
kind: Service
metadata:
  name: {{PIPELINE_BUILD_REGISTRY_NAMESPACE}}-{{PIPELINE_MODEL_TYPE}}-{{PIPELINE_MODEL_NAME}}-{{PIPELINE_MODEL_TAG}} 
  labels: 
    app: {{PIPELINE_BUILD_REGISTRY_NAMESPACE}}-{{PIPELINE_MODEL_TYPE}}-{{PIPELINE_MODEL_NAME}}
    version: {{PIPELINE_MODEL_TAG}}
spec:
  ports:
  - port: 6969
    name: http-nginx
  selector:
    app: {{PIPELINE_BUILD_REGISTRY_NAMESPACE}}-{{PIPELINE_MODEL_TYPE}}-{{PIPELINE_MODEL_NAME}} 
    version: {{PIPELINE_MODEL_TAG}}
